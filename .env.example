# Supabase → Project Settings → API
# Use ONLY the public anon (or publishable) key. Never the service_role / secret key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: storage plan size in MB, for the "espacio usado" meter (default 1024 = Supabase free plan).
# VITE_STORAGE_LIMIT_MB=1024
